services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: klyrh
      POSTGRES_PASSWORD: klyrh_dev
      POSTGRES_DB: klyrh
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
